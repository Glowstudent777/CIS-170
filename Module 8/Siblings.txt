// Author: Glowstudent
// Program: Siblings

Declare Integer siblings

Display "Enter number of siblings: "
Input siblings

If isInteger(siblings) Then
    If siblings >= 0 Then
        If siblings == 0 Then
            Display "Ah you are an only child."
            Return
        Else
            // TODO: Loop Stuff using Repetions
    Else
        Display "Invalid entry for number of siblings! Program is exiting!"
        Return
Else
    Display "Invalid entry for number of siblings! Program is exiting!"
    Return
End If

Return